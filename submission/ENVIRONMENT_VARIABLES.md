# Environment variables

## Backend

- `PORT` → backend port
- `MONGO_URI` → MongoDB connection string
- `DB_NAME` → MongoDB database name
- `JWT_SECRET` → token signing secret
- `FRONTEND_URL` → allowed frontend origin

## Frontend

- `REACT_APP_API_URL` → deployed backend base URL
